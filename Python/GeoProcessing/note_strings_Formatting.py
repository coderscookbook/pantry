# 'r' used for raw strings that don't interpret escape characters
# 'f' used for formatted strings that take in variables and escape characters

# string.strip() == removes trailing and leading whitespace